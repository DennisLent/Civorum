[project]
name = "stratega2"
version = "0.1.0"
description = "Python SDK scaffold for Civorum benchmark agents."
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

